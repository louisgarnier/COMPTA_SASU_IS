# Build Log — Session History
> Generated by following `docs/project/requirements/6-BUILD.md`

## 2026-07-01 — S1.1 Scaffold back + front

**Story :** EPIC-1 / S1.1 — squelette FastAPI `/health` + Next.js, lancement 1 commande.

**Fait :**
- `backend/api/main.py` remis aux couleurs LGC : titre `LGC API`, endpoints `/` + `/health`, CORS `localhost:3000`. **Retrait de l'appel DB au startup** (`init_database`/lifespan) — le back démarre sans base (la DB = S1.2).
- **Suppression du scaffold DB générique** hérité du template : `backend/database/`, `backend/api/models.py`, `backend/api/routes/example.py`, `backend/database/schema.sql`, `backend/tests/test_database.py`. (Recréé proprement en S1.2 sous `backend/db/` selon archi §5.)
- Front rebrandé LGC : `app/page.tsx` (titre LGC + indicateur d'état backend via ping `/health`), `src/api/client.ts` nettoyé. Import corrigé `@/api/client` (l'alias template `@/src/api/client` était cassé vs `tsconfig` `@/* → ./src/*`).
- **`Makefile`** : `make dev` (uvicorn :8000 + next :3000 en parallèle, trap kill), `make test`, `make back/front`, `make install`.
- Tooling test front réparé : conflit peer deps (`@testing-library/react@14` exige React 18, projet en React 19) → bump v16 + ajout `@testing-library/dom`. Config jest consolidée dans `frontend/jest.config.js` (next/jest, `dir: './'`), suppression de la config racine morte.

**Preuves :**
- Back tests : `4 passed` (`test_root_endpoint`, `test_health_endpoint`, `test_cors_allows_frontend_origin`, `test_config_has_defaults`).
- Front tests : `2 passed`, zéro warning act().
- `make test` (commande unique) : back 4 + front 2, tout vert.
- Live via `make dev` : `GET /health` → `200 {"status":"healthy"}`, `GET /` → `200 {"message":"LGC API","status":"ok"}`, CORS `access-control-allow-origin: http://localhost:3000` présent, page front sert `LGC` / « Suivi cashflow SASU » / « État du backend » en HTTP 200. Aucune init DB au startup (log uvicorn).

**Notes / dette :**
- Next a auto-modifié `frontend/tsconfig.json` (`jsx: react-jsx`, `.next/dev/types`) au 1er `dev` — normal.
- Warning `[baseline-browser-mapping] data over two months old` sur les commandes front — cosmétique.
- `CLAUDE.md` projet contient encore des placeholders `[NAME]`/stack — à remplir hors S1.1.

**Prochaine story :** S1.2 — Couche base de données (SQLAlchemy + Alembic + SQLite, entités archi §4, montants `Decimal`).
